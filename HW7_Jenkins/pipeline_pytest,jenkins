stage("test PythonEnv") {
    agent any

    stages {
        stage('pytest') {
            steps {
            sh 'pip install pytest'
            sh 'pytest HW6_pytest/pytest/test_sql_server_db.py'
            }
         }
}